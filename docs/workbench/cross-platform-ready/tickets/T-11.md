---
id: T-11
title: README 补 mac / Windows 的用法
blocked-by: [T-07]
owner: ~
status: done
files: [README.md]
---

README 现在整篇以 niri 为中心：快捷键表写的是 `Mod+Shift+T`，安装章节是 `pac` + `install.sh`
+ niri 配置。三平台可用之后这会误导 mac/Windows 用户。

要补：三家各自的安装方式（Linux 编译安装 / mac 装 .dmg / Windows 装 .msi）、
各自的快捷键（mac 与 Windows 是 `Alt+Shift+T`）、mac 的辅助功能授权步骤。

依赖 T-07：快捷键如果做成可配置的，README 的写法要跟着变。

**验收**：三个平台的用户都能只看 README 把它用起来。

---

## 已完成（2026-08-20）

外科式修改，只动会**误导 mac / Windows 用户**的部分，没有整篇重写（README 的 Linux 细节
本身没错，是这个项目的出身）。

改了：开头一句（不再自称"niri / Wayland 下的"）、快速开始（三平台各一节，mac 那节把
辅助功能授权和"升级后要重勾"讲在前面）、快捷键表（三列 + 说明 mac/Windows 可改键而
Linux 不行及原因）、配置路径（三平台各一行）、排查表（给每条标上适用平台，补 mac 授权
和快捷键被占两类）、原理章节（新增三平台取词对照表）。

**顺手修掉四条已经过时的说法**（都是这两天自己改出来的）：

1. 「mac / Windows 的取词没有真机验证过」——mac 已经验过了
2. 「mac 的托盘图标不是模板图标」——已经是了
3. 「Windows 传统控制台里取不到词」——已被 Ctrl+Insert 治好
4. 「不想冒剪贴板的险就设成『仅主选区』（只有 Linux 有）」——现在三家都有对应的零副作用
   路线，补了对照表

**验证状态：已验证**（`pnpm build` 通过；标题结构和 details 块逐个核对过）。
