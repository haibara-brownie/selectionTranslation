---
id: T-12
title: Linux 回归
blocked-by: []
owner: ~
status: done
files: []
---

## 结果（2026-08-20 晚，Arch / niri，真机）

**九条全过。** 逐条证据见文末「回归结果」。另外查出两个票外问题，都已修：

1. **本票开头写的 `cargo build --release` 是错的** —— 那样编出来的 Tauri 二进制
   **跑不起来**（连 devUrl，窗口空白 + `Connection refused`）。Tauri 靠 `tauri`
   依赖有没有开 `custom-protocol` 判断 dev/生产，只有 `tauri build` 会加。
   已改 README 与 `docs/切到-tauri-版.md`，下面的命令也已订正。
2. **Tauri 的 niri 窗口规则从没装到本机** —— 仓库 `data/niri-snippet.kdl` 里有，
   但用户的 `~/.config/niri/selectiontranslation.kdl` 还是只有 GTK 那两条
   （`install.sh` 是安装动作，验证过程中不该跑）。装上之后第 4 条才成立。
   `install.sh` 是整份覆盖该文件的，重跑就会带上，不存在漂移。

本轮（2026-08-20，Mac 侧）改了大量**三平台共用**的代码：`windows.rs`、`main.rs`、
整个前端。mac 上逐条验过了，Linux 一次都没跑过。**这张票是本 feature 的最后一关。**

## 先跑起来

Arch 上和 Mac 不同：**能编全部三个 package**，不需要 `--exclude seltrans`。

```bash
git pull
pnpm install                      # 前端依赖没变，但保险
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
pnpm build
cargo build --workspace --release # 注意 --workspace，否则只编根包
pnpm tauri build --no-bundle      # Tauri 二进制必须这样编，理由见文首
```

然后跑 Tauri 版（**不要覆盖你日常在用的 `~/.local/bin/seltrans`**，那是 GTK 版）：

```bash
./target/release/seltrans-tauri tray &
```

## 必须回归的（按重要性排，每条都注明为什么）

### 1. 常驻后**第一次**按快捷键就要出译文

修复提交 `e44c260`。`open_popup` 新建窗口那条路上原本没把取到的文本写进 `Launch` 槽位，
表现是常驻起来后第一次触发开出一个**空弹窗**，第二次才正常。

**Linux 上这是日常主路径**（合成器每次按键都 spawn 新进程 → single-instance 递给常驻实例
→ 新建或复用窗口）。mac 上验过了，Linux 必须再验一遍。

> 验法：`pkill seltrans-tauri`，重新 `tray &`，选中一段文字，**按第一次**就该出译文。

### 2. 一次触发只取一次词

修复提交 `058a454`。原先新进程在 `tauri::Builder` 之前取一次、常驻实例回调里又取一次。
**Linux 是重灾区**：合成器每次按键都 spawn 新进程，所以每次都双取；一旦掉进 Ctrl+C
兜底，就是两轮「抬修饰键 → 模拟复制 → 等剪贴板 → 还原」，用户剪贴板被改写还原两遍。

> 验法：`tail -f` 日志，按一次快捷键，**「开始取词」只该出现一次**。
> 特别要试一个不支持主选区的应用（某些 Electron），确认「模拟 Ctrl+C」也只出现一次。

### 3. 关掉设置窗口，托盘不能跟着没

修复提交 `4083345`。Tauri 默认「所有窗口都关了就退出」；弹窗靠 `prevent_close` 绕开了，
设置窗口是真销毁的，于是按一次 Esc 整个程序退出——托盘图标消失、快捷键失效。

> 验法：`tray &` 之后打开设置页，按 Esc 关掉，**托盘图标还在、快捷键还能用**。

### 4. 弹窗位置仍归 niri 管

新加的 `place_top_right()` 是 `#[cfg(not(target_os = "linux"))]`，理论上 Linux 不编译它。
但要确认弹窗**还是老样子出现在右上角**，没有被应用自己摆位干扰。

### 5. 自绘下拉在 WebKitGTK 上的表现

**这是本轮改动面最大的一块**，原来用的是原生 `<select>`。四个点都要看：

- 顶栏「翻译风格」和底栏「模型」都能展开，**浮层不被裁**（浮层现在挂在 `document.body`
  上用 `position: fixed`，就是为了逃开祖先的 `overflow: hidden`）
- 底部那个下拉空间不够时**向上翻**
- 设置页四个下拉（目标语言/默认提示词/取词方式/配色）以及字体那三个搜索框
- 键盘：Tab 聚焦、空格展开、↑↓ 移动、Enter 选中、Esc 只收下拉不关窗

### 6. 动效在 WebKitGTK 上不卡

弹窗进场/退场、译文流式渐现、骨架屏、切风格时的交叉淡出。
mac 上是 WKWebView，Linux 是 WebKitGTK，同一套 CSS 未必一样顺。
**尤其看首字延迟有没有变慢**——分片渐现的起始透明度刻意设成 0.15 就是为了这个。

### 7. 新的小尺寸托盘图标（**Linux 专属，mac 上看不到**）

新增 `data/tray-small.svg`，ksni 在 22/32 两档用它，48/64 仍用应用图标。
这份图**只有 Linux 会用**，Mac 侧只能靠单测（`cargo test -p seltrans-tauri` 里的
`托盘小尺寸图在_22px_下箭头仍是可辨的镂空`）间接保证，**观感必须你在 niri 面板上看**。

> 看不清就调 `data/tray-small.svg`，改完 `cargo test -p seltrans-tauri` 会告诉你有没有
> 把箭头调没。

### 8. 快捷键：Linux 分支不该出现录制控件

`Mod+Shift+T` / `Mod+Alt+T` 应完全不受影响（本轮只改了非 Linux 分支的默认值）。
设置页的「快捷键」分组在 Linux 上**应当列出 niri 提供的按键 + 说明为什么改不了**，
而不是显示 mac/Windows 那两个录制按钮。

### 9. 「关于」页的依赖自检

Linux 分支应仍显示 wl-clipboard / ydotool / niri 规则的状态。

## 完成之后

结果记进 `cross-platform-ready/progress.log.md`，把本票 `status` 改成 `done`，
然后这个 feature 就可以写交付报告了（HEAVY 阶段 9）。

发现新问题就新开票，别直接在这张里堆。

---

## 回归结果（逐条）

| # | 条目 | 结论 | 怎么验的 |
|---|---|---|---|
| 1 | 常驻后第一次触发就出译文 | ✅ | `tray` 起来后第一次 `popup`，日志一路走完 取词→发起翻译→翻译完成，弹窗有译文 |
| 2 | 一次触发只取一次词 | ✅ | 同一次触发的日志里「开始取词」只出现 1 次 |
| 3 | 关设置窗口托盘不掉 | ✅ | Esc 关掉设置后进程存活、SNI 仍注册；后续 `popup` 照常翻译。误触 ✕ 时又复现了一次 |
| 4 | 弹窗位置仍归 niri 管 | ✅ | 装上规则后 `is_floating=True`、`size=[560,480]`、右上角；设置窗口按标题匹配到 900×700 |
| 5 | 自绘下拉在 WebKitGTK 上 | ✅ | 顶栏风格下拉展开不被裁；底栏模型下拉**向上翻**；设置页目标语言下拉盖过好几行、没被滚动容器裁掉；空格展开 / ↑↓ 移动 / Enter 选中 / Esc 只收下拉不关窗；标签栏 Tab+空格可切页 |
| 6 | 动效不卡 | ✅ | wf-recorder 录 60fps + ffmpeg 抽帧：骨架屏三条微光在扫、spinner 在转、首 token 到达后骨架屏换成文字并出现闪烁光标（光标只在有文字后出现，那个修复生效） |
| 7 | 小尺寸托盘图标 | ✅ | 面板上放大 8 倍看：深色圆角方块 + 薄荷绿选区条 + 深色箭头镂空，箭头清晰可辨 |
| 8 | Linux 不出现录制控件 | ✅ | 「快捷键」分组是只读列表（Mod+Shift+T / Mod+Alt+T / Esc / F5 / …），无录制按钮 |
| 9 | 依赖自检 | ✅ | 四项全绿：wl-clipboard、ydotool（服务在跑）、niri 规则、运行日志；汇总条「依赖齐全」 |

### 验证手法（Linux 侧，补充 Mac 那份）

- **`wl-copy --primary "..."` 可以直接造出「划过词」的状态**，不用真去选中文字，
  也不碰用户的剪贴板（主选区本来就是易失的）。整条快捷键通路都能这么驱动。
- **动画同样要录像抽帧**：`wf-recorder -f x.mp4 -r 60`，再
  `ffmpeg -ss <t> -t <d> -i x.mp4 -vf "crop=W:H:X:Y,fps=N,scale=..,tile=RxC" -frames:v 1`。
  注意 wf-recorder 的 `-g` 在本机没限制住录制区域，录的是整屏，靠 ffmpeg 的 crop 裁。
- **发按键前必须先确认焦点**：`niri msg -j windows` 里找 `is_focused`。
  显示器休眠 / 会话锁定时**整个会话没有任何窗口持有焦点**，那时候发键既证明不了
  什么又有卡键风险。按键一律走 `docs/workbench/tools/safekey.sh`（带 trap，
  正常/报错/中断/被杀四种退出都保证抬起全部修饰键）。
- **不要用 `pkill -f`** 关自己起的进程 —— 模式会匹配到 Bash 工具自己的 shell，
  退出码 144。用 `pkill -x <名字>` 或 niri 的 `close-window --id`。
