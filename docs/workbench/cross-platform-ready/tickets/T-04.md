---
id: T-04
title: 动效 · 加载态与骨架屏
blocked-by: []
owner: ~
status: done
files: [ui/popup.ts, ui/style.css, ui/index.html]
---

等首个 token 的那几百毫秒现在是空白，只有一个 spinner。要给个有生命的占位。

**验收**：从按下快捷键到首字出现的整段时间里，译文区不是空白。

---

## 已完成（2026-08-20）

三条宽度递减的微光条，`setBusy(true)` 时铺上，第一个分片到达时撤掉；
`done` 事件里再兜一次（空响应时一个分片都不会来）。

**验证状态：已验证。** 截图拍到了等待期的骨架屏——三条微光条 + spinner + 「翻译中…」，
输出区不再是空白；译文到达后骨架屏正确撤除。
