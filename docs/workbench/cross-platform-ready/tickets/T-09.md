---
id: T-09
title: 发版说明补 TCC 授权会随升级失效
blocked-by: []
owner: ~
status: done
files: [docs/发版.md, .github/workflows/release.yml]
---

实测：**重新打包后 cdhash 变化，macOS 的辅助功能授权失效**，用户每次升级都要重新勾选。
签名+公证的 app 不会有这个问题（designated requirement 基于 Team ID）。

`docs/发版.md` 已有「未签名怎么绕过」一节，需补上这条；`release.yml` 里写给用户的
Release 说明（那段 heredoc）也要同步。

不补的话，用户升级后会以为程序坏了。

**验收**：发版说明里能找到「升级后要重新授权辅助功能」及原因。

---

## 已完成（2026-08-20，提交 9589f14）

`docs/发版.md` 和 `release.yml` 里给用户的 Release 说明都补上了「升级后要重新授权
辅助功能」及原因（未签名应用 cdhash 每次构建都变，TCC 据此作废授权）。

顺带修掉 `release.yml` 里一句假话：原本写「macOS / Windows 上在设置里直接配」快捷键，
但设置界面根本没有改键入口（那是 T-07）。改成如实说明当前是写死的
`Alt+Shift+T` / `Alt+Shift+,`，被占用了暂时改不了。
